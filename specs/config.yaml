schema: spec-driven

project: vaultax
description: |
  HashiCorp Vault integration for .NET 10+ with transparent config overlay, multiple auth methods, and KV/Transit/PKI engines

context: |
  Stack: .NET 10 / C# 14, VaultSharp 1.17.5.1
  Architecture: Single NuGet package
  Testing: xUnit (integration tests need live Vault)
  Style: TreatWarningsAsErrors, no implicit usings

rules:
  proposal:
    - Include rollback plan for risky changes
    - Identify affected domains
  specs:
    - Use Given/When/Then format for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
    - Delta specs use ADDED/MODIFIED/REMOVED sections
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase (foundation, core, integration, testing)
    - Hierarchical numbering (1.1, 1.2)
    - Small enough to complete in one session
  apply:
    - Follow existing code patterns and conventions
  verify:
    - Run tests if test infrastructure exists
    - Compare implementation against every spec scenario
  archive:
    - Merge delta specs into main specs
    - Warn before merging destructive deltas
